Dla jasności : tak jest to cały projekt Visuala. Żeby ogarnąć jak działa to API i WEB service restowy musiałem takowy stworzyć i posiada on własną konfigurację dla bazy danych (niestety nie mam pliku naszej, więć musiałem lekko improwizować i stworzyłem póki co logikę (kontrollery, odpowiadające im obiekty itd...) dla użytkowników i hostów. 

po odpaleniu ( ja mam VS 12  ale zalecana jest raczej 13+ - niektóre funkcje mi nie działają i straciłęm przez to sporo czasu bo da się trochę rzeczy robić mniej manualnie) 
jeżeli pojawi się strona główna to można odpytywać serwer poprzez dopisanie 

api/hosts api/hosts/1 itd
api/users api/users/1 itd.

